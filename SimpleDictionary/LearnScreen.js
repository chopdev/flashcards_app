import React from 'react';
import { View, Text, Button } from 'react-native';

const LearnScreen = () => {
  return (
    <View>
      <Text>Flash cards here</Text>
      {/* Add settings options */}
    </View>
  );
};

export default LearnScreen;